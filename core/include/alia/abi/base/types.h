#ifndef ALIA_C_BASE_TYPES_H
#define ALIA_C_BASE_TYPES_H

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>

/*
  Alia deliberately uses standard C99 integer and boolean types
  (uint32_t, size_t, bool, etc.) in its public C API.

  This header exists to:
  - centralize those includes
  - define Alia-owned semantic types (IDs, flags, status)
*/

#endif /* ALIA_C_BASE_TYPES_H */
